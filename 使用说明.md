# 书游引擎 · 使用说明

> 一个「阅读为主、影响为辅」的互动小说网页应用。导入一本 txt 小说，正常阅读原文；随时插入你的行动，影响剧情走向——**原文永不改写，影响用旁注/支线呈现**。

---

## 一、这是什么？它怎么工作？

一句话：**服务器只放静态文件，大脑（数据 + 逻辑）全在你的浏览器里。**

所以它有这些天然特性：

- **看书 0 消耗**：只有复杂行动才调 AI；
- **数据本地化**：你的书、存档、NPC 记忆，都存在你自己浏览器的 IndexedDB 里；
- **多人互不干扰**：谁打开网页，数据就存在谁的浏览器里，彼此完全独立；
- **API Key 不上传**：填在你自己浏览器里，服务器不保存。

---

## 二、快速开始（三步上手）

### 第 1 步：导入小说
打开网页，把一本 `.txt` 小说**拖进**页面（或点「＋导入」选择文件），它会自动按「第X章 / Chapter X」分章。支持多本书，顶部书架随时切换。

### 第 2 步：开局选角色
导入后自动进入「开局」界面：
- 点「废柴逆袭 / 世家天才 / 市井白手」模板**一键填充**，或手动填写；
- 给角色起个名字 → 点「开始游玩」；
- 想换角色？点「＋新存档」开新档（一个存档锁定一个角色，换角色只能开新存档）。

### 第 3 步：阅读 + 行动
- 点「📑 目录」看章节目录，点哪章跳哪章；「←/→」或键盘 **← / →** 键翻章；
- 底部输入框输入行动，回车提交——这就是「玩」的核心。

---

## 三、核心玩法：插入行动

行动分三级，消耗不同：

| 级别 | 例子 | 处理方式 | 消耗 |
|---|---|---|---|
| **简单** | 打听、观察、闲聊、移动 | 本地规则秒回 | **0 token** |
| **中等** | 结交、试探、谈判 | 本地规则 + 更新关系 | 0 token |
| **复杂** | 建势力、干预主线、改变关键关系 | 调 AI 判断 | 按次计费 |

### 💡 旁注玩法（把影响锚定到正文）
1. 用鼠标**选中正文里的某句话**；
2. 底部输入行动、提交；
3. 那句话的段落末尾会长出图标：💡 简单/中等行动，📈 复杂行动；
4. 点图标，展开看这次行动的结果，再点收起。

### 影响会体现在哪？
- **主线偏移度**：简单 +0，中等 +0.05，复杂 +0.2；分级从「自由支线」到「剧情事件拉回」；
- **NPC 记忆**：每张关系卡记录态度、信任度、关系变化史。

---

## 四、成长面板

点「📋 角色」打开侧栏，6 个标签页实时更新：

| 标签 | 内容 |
|---|---|
| 档案 | 姓名 / 身份 / 阵营 / 开局设定 / 当前章节 |
| 能力 | 固有技能 / 人脉 / 资源 |
| 势力 | 势力名 + 掌控资源 |
| 履历 | 大事（复杂行动）+ 流水账（全部行动） |
| 关系 | 每个 NPC 一张卡：态度 / 信任度 / 关系史 |
| 偏移 | 当前偏移度数值 + 渐变条 + 分级说明 |

---

## 五、接入 AI（复杂行动才需要）

只有「复杂行动」需要 AI。点右上角 ⚙️ 设置，两条路：

### 方式 A：DeepSeek（云服务，便宜，推荐）
1. 打开 https://platform.deepseek.com 注册登录；
2. 左侧「API Keys」→「创建 API Key」→ 复制 `sk-...`；
3. 回到书游引擎 ⚙️ 设置：
   - **API Key**：粘贴 `sk-...`
   - **接口地址**：`https://api.deepseek.com/v1`
   - **模型**：`deepseek-chat`
   - **每日预算**：填上限（如 `1` 元），超了自动停
4. 保存。之后复杂行动就真正调 AI 了，每次会显示 token 和费用。

### 方式 B：Ollama（本地，免费，无需 Key）
1. 安装 https://ollama.com ，运行 `ollama run qwen2.5:7b`；
2. ⚙️ 里：接口地址填 `http://localhost:11434/v1`，模型填 `qwen2.5:7b`，API Key 留空。

> ⚠️ **API Key 只存在你自己的浏览器 localStorage 里**，绝不上传服务器，也不在代码/仓库里。发给别人用，别人看不到、也用不到你的 Key。

---

## 六、导出 / 导入 / 清数据

- **导出**：成长面板右上「⤓ 导出」→ 档案（Markdown）/ 影响线（Markdown）/ 存档备份（JSON）；
- **换设备**：① 新设备先导入原 txt → ② ⚙️ 设置里「导入存档备份」恢复；
- **清除所有数据**：⚙️ 设置 → 数据管理 → 「清除所有数据」。

---

## 七、常见问题（FAQ）

**Q：把网址转发给别人用，会影响我的电脑 / 内存吗？**
不会。它是纯静态网页：别人打开后，一切都在**他自己的浏览器**里跑，数据存他自己的浏览器，跟你的电脑零关系。网站文件在 GitHub 的服务器上，不在你电脑上。

**Q：别人用会不会花我的钱 / 用我的 API Key？**
不会。AI 费用从**填了 Key 的那个人**的账户扣。你的 Key 只在你自己的浏览器里。

**Q：为什么复杂行动要 API，简单行动不用？**
简单行动走本地规则库，0 成本。复杂行动需要「理解剧情并判断后果」，这必须靠 AI，所以按次计费。

**Q：数据安全吗？换浏览器会丢吗？**
数据存在浏览器 IndexedDB，清浏览器缓存会清掉。建议用「导出存档」定期备份。

---

## 八、本地运行 / 部署

```bash
npm install
npm run dev        # 本地运行，打开 http://localhost:3000
npm run build      # 生成静态文件到 out/，可部署
```

部署到 Vercel / GitHub Pages / Netlify 详见 [README.md](README.md)。
