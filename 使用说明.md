# 书游引擎 · 使用说明

> 一个「阅读为主、影响为辅」的互动小说网页应用。导入一本 txt 小说，正常阅读原文；随时插入你的行动，影响剧情走向——**原文永不改写，影响用旁注/影响面板呈现**。

---

## 一、这是什么？它怎么工作？

一句话：**服务器只放静态文件，大脑（数据 + 逻辑）全在你的浏览器里。**

所以它天生具备：

- **看书 0 消耗**：只有复杂行动才调 AI；
- **数据本地化**：书、存档、NPC 记忆、系统数值，都存在你自己浏览器的 IndexedDB 里；
- **多人互不干扰**：谁打开网页，数据就存在谁的浏览器里，彼此独立；
- **API Key 不上传**：填在你自己浏览器里，服务器不保存。

---

## 二、快速开始（三步上手）

### 第 1 步：导入小说
把一本 `.txt` 小说**拖进**页面（或点「＋导入」），自动按「第X章 / Chapter X」分章（UTF-8 和 GBK 编码都能识别）。支持多本书，顶部书架切换。

### 第 2 步：开局选角色
导入后自动进入「开局」界面：
- **配了 AI**：会自动分析书籍类型（修仙/后宫/权谋…），生成 **3 个符合世界观的身份**给你选；
- **没配 AI**：用通用模板（废柴逆袭/世家天才/市井白手）；
- 起个角色名 →「开始游玩」；想换角色 =「＋新存档」开新档。

### 第 3 步：阅读 + 行动
- 「📑 目录」看章节目录，「←/→」或键盘 **← / →** 翻章；
- 底部输入框输入行动，回车提交——这就是「玩」的核心。

---

## 三、核心玩法：插入行动

行动分三级，消耗不同：

| 级别 | 例子 | 处理方式 | 消耗 |
|---|---|---|---|
| **简单** | 打听、观察、闲聊、移动 | 本地规则秒回 | **0 token** |
| **中等** | 结交、试探、谈判 | 本地规则 + 更新关系 | 0 token |
| **复杂** | 建势力、干预主线、改变关键关系 | 调 AI 判断 | 按次计费 |

### 💡 旁注玩法（把影响锚定到正文）
1. 用鼠标（或手机长按）**选中正文里的某句话**；
2. 输入行动、提交；
3. 那句话末尾长出图标：💡 简单/中等行动，📈 复杂行动；
4. **点图标 → 右侧弹出「影响面板」**，高亮显示这次行动的结果。

### 📈 影响面板
点顶部「📈 影响」按钮（或点正文里的图标）打开，里面显示：
- **主线偏移度**（数值 + 渐变条 + 分级）；
- **全部行动影响记录**（内容、级别、token 消耗、结果）。

### 影响体现在哪？
- **主线偏移度**：简单 +0，中等 +0.05，复杂 +0.2；分级从「自由支线」到「剧情事件拉回」；
- **NPC 记忆**：每张关系卡记录态度、信任度、关系变化史。

### 输入框小技巧
- 右下角可**拖拽调高度**；
- 点「∨ 收起」变成一条「💬 输入行动…」，点开才展开，不挡阅读。

---

## 四、成长面板

点「📋 角色」打开侧栏，7 个标签页实时更新：

| 标签 | 内容 |
|---|---|
| 档案 | 姓名 / 身份 / 阵营 / 开局设定 / 当前章节 |
| 能力 | 固有技能 / 人脉 / 资源 |
| 势力 | 势力名 + 掌控资源 |
| 履历 | 大事（复杂行动）+ 流水账（全部行动） |
| 关系 | 每个 NPC 一张卡：态度 / 信任度 / 关系史 |
| 偏移 | 当前偏移度数值 + 渐变条 + 分级说明 |
| 系统 | 等级 / 经验 / 点数 / 兑换 |

---

## 五、系统面板（经验 / 点数 / 兑换）

- **经验获取**（全本地，0 token）：读新章节 +5、简单行动 +2、中等 +8、复杂 +20；
- **升级**：100 经验升 1 级，每级送 10 点数；
- **兑换**（花点数）：NPC 情报 / 补充资源 / 临时技能 / 势力加成；
- **约束**：偏移度 ≥0.8 时系统拒绝兑换（不能破坏主线）。

---

## 六、存档点（手动 + 自动）

点顶部「💾 存档」打开存档面板：

- **自动存档**：每翻到新的一章自动存一次（保留最近 3 个自动存档）；
- **手动存档**：填个名字点「＋存档」，可存任意时刻；
- **读档**：玩崩了（比如偏移度太高），点旧存档的「读档」回退到那时；
- **删除**：清理不需要的存档点。

> 存档点存的是「当前游戏状态快照」（章节、偏移度、NPC 记忆、系统数值、履历），存在浏览器本地。

---

## 七、搜索与统计

- **🔍 搜索**：点顶部「🔍 搜索」→ 输入关键词 → 全文搜索当前书，点结果跳到对应章节；
- **📊 统计**：点顶部「📊 统计」→ 看章节进度 / 行动次数 / 关系数 / 等级 / 点数 / 偏移度，以及一条**偏移度趋势曲线**（虚线标出 0.3 / 0.6 / 0.8 分级线）。

---

## 八、接入 AI

「复杂行动」和「开局身份生成」需要 AI。点右上角 ⚙️，顶部有 **4 个「AI 来源」按钮，一键切换**：

| 选项 | 说明 |
|---|---|
| 🖥️ 本地 Ollama | 免费，用你电脑算力（需先装 Ollama + 下模型） |
| ☁️ DeepSeek | 云服务，便宜，需填 API Key |
| 🔧 自定义 | 手动填接口地址和模型 |
| ⛔ 关闭 AI | 复杂行动走占位，不调 AI |

按钮下方会显示当前状态：**✅ 本地免费 / 💳 云计费 / ⛔ 已关闭**，一眼看清烧的是哪个。

### 本地 Ollama（免费）
1. 装 [Ollama](https://ollama.com) + 下载模型 `ollama pull qwen2.5:7b`；
2. 保证 Ollama 在运行（开始菜单打开）；
3. ⚙️ 点「本地 Ollama」→ 自动填好 → 保存。

### DeepSeek（云服务，便宜）
1. 到 [platform.deepseek.com](https://platform.deepseek.com) 建 API Key；
2. ⚙️ 点「DeepSeek」→ 填 Key → 保存。

> ⚠️ **API Key 只存在你自己的浏览器 localStorage 里**，绝不上传服务器，也不在代码/仓库里。
> 注意：本地 Ollama 只在本机（`localhost`）上有效；部署到网上的网址（github.io）用 DeepSeek。

---

## 九、导出 / 导入 / 清数据

- **导出**：成长面板右上「⤓ 导出」→ 档案（Markdown）/ 影响线（Markdown）/ 存档备份（JSON，含角色、记忆、行动、系统面板、存档点）；
- **换设备**：① 新设备先导入原 txt → ② ⚙️ 里「导入存档备份」恢复；
- **清理垃圾数据**：⚙️ → 数据管理 → 「清理垃圾数据」，删除无主的孤儿数据（打开应用也会自动清理一次）；
- **清除所有数据**：⚙️ → 数据管理 → 「清除所有数据」。

---

## 十、常见问题（FAQ）

**Q：把网址转发给别人用，会影响我的电脑 / 内存吗？**
不会。纯静态网页：别人打开后一切都在他自己的浏览器里跑，数据存他自己的浏览器，跟你的电脑零关系。

**Q：别人用会不会花我的钱 / 用我的 API Key？**
不会。AI 费用从填了 Key 的那个人账户扣，你的 Key 只在你自己的浏览器里。

**Q：手机能用吗？**
能。已适配移动端（viewport、触摸选中、面板全屏抽屉），电脑手机都能用。

**Q：数据安全吗？换浏览器会丢吗？**
数据在浏览器 IndexedDB，清浏览器缓存会清掉。建议用「导出存档」或「存档点」定期备份。

---

## 十一、本地运行 / 部署

```bash
npm install
npm run dev        # 本地运行，打开 http://localhost:3000
npm run build      # 生成静态文件到 out/，可部署
```

部署到 Vercel / GitHub Pages / Netlify 详见 [README.md](README.md)。
